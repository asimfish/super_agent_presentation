# Release card: <model, dataset, or system name and version>

<!-- Combined model-card / datasheet summary for releasing or handing off a
trained model, dataset, or evaluated system. Fill only applicable sections and
delete the rest; never leave a placeholder. State `not evaluated` explicitly
rather than omitting a row silently. Remove all comments before delivery. -->

## Identity and access

<!-- REQUIRED: Name, version, date, license, persistent locator (repository,
DOI, or registry path), and contact or owner. Access constraints and intended
audience. "Available on reasonable request" is not an access path. -->

## Intended use and out-of-scope use

<!-- REQUIRED: Primary intended uses and users; foreseeable misuses and uses
the artifact was not designed or evaluated for. -->

## Composition and provenance

<!-- REQUIRED for datasets, RECOMMENDED for models: What the data contains,
where it came from, collection window and method, preprocessing and filtering
steps with counts at each exclusion step, known gaps, and consent or licensing
basis. For models: training data identity, size, and cutoff. -->

## Evaluation

<!-- REQUIRED: Metrics with definition and direction, evaluation data and
protocol, results disaggregated across relevant slices (population groups,
domains, conditions), and uncertainty with run counts. Point to the full
experiment report rather than duplicating it. -->

## Known limitations and failure modes

<!-- REQUIRED: Observed failure modes, slices with degraded performance,
distribution shifts that break the artifact, and safety or fairness caveats.
An empty section is a red flag, not a clean bill. -->

## Reproduction

<!-- RECOMMENDED: Code location and commit, environment, configuration, seeds,
and compute used. State what a third party can reproduce with the released
artifacts and what remains internal. -->

## Maintenance

<!-- OPTIONAL: Update policy, deprecation plan, versioning scheme, and how to
report issues. -->
