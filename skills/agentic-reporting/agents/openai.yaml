interface:
  display_name: "Agentic Reporting"
  short_description: "Outcome-first, evidence-calibrated reports"
  default_prompt: "Use $agentic-reporting to present this result in the clearest task-appropriate format."
policy:
  allow_implicit_invocation: true
