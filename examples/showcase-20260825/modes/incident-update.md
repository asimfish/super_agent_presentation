# Synthetic API incident update

**截至 synthetic UTC 09:23｜状态：已恢复、仍在监控，尚未 resolved。** p95 从 1.9 s 回到 440 ms，但错误率与数据完整性检查尚未完成。

本报告仅使用合成展示数据，不代表真实研究或生产结果。

## 影响与当前证据

09:10 起，200 个合成请求中 18 个超过 2 s；参考 p95 为 400 ms。09:23 后的 100 个请求 p95 为 440 ms。证据只覆盖给定合成窗口，见[本地 incident 包](../inputs/incident-update.md)。

## 已采取动作

- 09:18 回滚新 batching path。
- 09:23 观察到 p95 恢复至 440 ms。

回滚后的时序改善支持 batching path 与延迟相关，但不确认完整根因；回滚可用并已执行，也不等于事故已经解决。

## 已知、未知与下一步

已知 latency 指标已恢复到接近参考值。未知项包括根因、错误率审计和数据完整性结论。下一步是完成这两项检查并持续观察 latency；只有稳定性和闭环检查都满足给定 closure criteria 后，才能把状态改为 resolved。下一更新时间未由证据包提供，因此不虚构时间承诺。
